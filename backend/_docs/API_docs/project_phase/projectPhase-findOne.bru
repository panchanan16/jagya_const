meta {
  name: projectPhase-findOne
  type: http
  seq: 3
}

get {
  url: {{URL.Entity}}project_phase/readone?pro_id=11
  body: json
  auth: bearer
}

params:query {
  pro_id: 11
}

auth:bearer {
  token: <your_token_here>
}

assert {
  ~status: isTrue
}
